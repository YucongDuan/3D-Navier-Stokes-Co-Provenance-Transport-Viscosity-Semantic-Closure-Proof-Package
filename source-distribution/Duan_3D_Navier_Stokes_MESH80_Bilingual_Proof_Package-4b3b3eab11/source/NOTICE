DIKWP-MESH 8.0 is a core semantic runtime.
Only D, I, K, W, and P have semantic-generative authority.
File formats, hashes, timestamps, CLI commands, test states, and dashboards are implementation mechanisms with zero semantic-generative authority.
The runtime records semantic content and explicit core-to-core changes. It does not automatically settle external domain claims.
